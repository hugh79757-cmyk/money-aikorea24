# 📌 Continue Here — 2026-07-01 16:30 KST

## Current Position
**Auto-writer 배포 버그 수정 완료**. build_deploy.py에 persona-stats.json 제거 로직 추가, 커밋/푸시/수동 배포 완료.

## Completed Work
- [x] auto-writer deploy 실패 원인 파악 (persona-stats.json 25MiB Cloudflare 제한)
- [x] `scripts/auto-writer/shared/build_deploy.py` 수정 — 빌드 후 `os.remove()` 추가
- [x] 수동 배포 실행 — 8개 글이 6/30, 7/1 생성됐으나 배포 안 됐던 것까지 모두 반영
- [x] `AGENTS.md`에 `버그 수정 기록` 섹션 및 pipeline 단계 12b 추가
- [x] 커밋 (`f174eab`, `04843c8`), 푸시 완료

## Remaining Work
- auto-writer DB: 5266 pending, 53 error — pipeline은 정상 가동 중
- launchd `kr.aikorea24.auto-writer` 매일 09:00 자동 실행 → 내일 아침 07/02 09:00에 자동 빌드+배포 예정
- `build_deploy.py` 수정 반영됐으므로 다음부터는 자동 배포 성공 예상

## Decisions
- `dist/persona-stats.json` 제거 전략: deploy.sh와 동일하게 `os.remove()` 사용
- manual-publisher의 `deployer.py`는 deploy.sh를 통해 배포하므로 별도 수정 불필요

## Blockers
- (none)

## Resume Instructions
1. `AGENTS.md` 먼저 읽어 프로젝트 컨텍스트 파악
2. `scripts/auto-writer/logs/scheduler.log`에서 최근 배포 성공 여부 확인
3. auto-writer DB에서 publish 상태 확인: `sqlite3 scripts/auto-writer/db/auto-writer.db "SELECT * FROM publish_ledger ORDER BY published_at DESC LIMIT 10;"`
4. 새 작업이 있으면 진행
