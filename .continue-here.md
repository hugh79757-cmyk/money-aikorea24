# .continue-here.md — 세션 핸드오프

> 중단 시각: 2026-07-10 (KST)
> 마지막 커밋: `cb16fb0` (기준) / `e60f788` (content: update)

## 현재 위치

**Phase 5 (완료) 이후 ad-hoc triage/fix 작업.** Phase는 모두 완료된 상태 (5/5, 100%). 이번 세션은 콘텐츠 품질 전수조사 및 재발방지 체계 구축.

## 이번 세션 완료

| 작업 | 상태 | 상세 |
|------|------|------|
| 블로그 200개 전수조사 | ✅ 완료 | 7개 체크리스트 스캔 — 194건 **목차**, 1건 중복 H1 |
| 배치 수정 — **목차** 제거 | ✅ 194 files | `build_summary_box()` 생성 TOC + `---` 구분선 일괄 제거 |
| 배치 수정 — 중복 H1 | ✅ 1 file | `청년-주거지원금-총정리.md` `#` → `##` |
| Root cause 제거 | ✅ `pipeline.py` | `build_summary_box()` 호출 비활성화 |
| Safety net | ✅ `validator.py` | `remove_ai_artifacts()` 추가 |
| Pre-build gate | ✅ `scripts/check-blog-issues.py` 신규 | 7개 항목 검증, `--ci`/`--fix` 모드 |
| deploy.sh 연동 | ✅ | 1/5 단계 pre-build check |
| build_deploy.py 연동 | ✅ | auto-writer 배포 전 check 선행 |
| Triage 등록 | ✅ | `20260710--blog-content-quality-enforcement` |

## 남은 작업

없음 (전수조사 + 재발방지 모두 완료).

## 주요 결정 (이번 세션)

| 결정 | 이유 |
|------|------|
| `build_summary_box()` 비활성화 | BlogPost.astro에 내장 TOC 존재. raw `**목차**`는 중복 렌더링 + `<hr>` 발생 |
| `remove_ai_artifacts()` validator에 추가 | 파이프라인 validate 단계에서 LLM 출력물 정화하는 안전장치 |
| pre-build gate를 CI 모드로 (`--ci` → exit 1) | 문제 발견 시 배포 중단, 사람 개입 전 문제 전파 방지 |
| batch fix는 offline 일회성 / pipeline은 online | 역할 분리: validator는 앞으로 생성될 글만 담당 |

## 블로커

- 없음. 모든 변경사항 커밋 대기 중.

## 재개 방법

```bash
cd /Users/twinssn/Projects/money-aikorea24

# 현재 상태 확인
git log --oneline -5
git status --short

# 커밋 (모든 변경사항)
git add -A && git commit -m "fix(blog): 전수조사 + **목차** 제거 + 재발방지 3단계 구축"

# 빌드 검증
npm run build

# 배포 (필요시 — deploy.sh가 자동 check → build → deploy)
scripts/deploy.sh

# launchd / auto-writer 상태
python3 scripts/auto-writer/scheduler.py --status
```

## 언커밋 변경

```
M  scripts/auto-writer/pipeline.py              # build_summary_box() 비활성화
M  scripts/auto-writer/validator.py             # remove_ai_artifacts() 추가
M  scripts/auto-writer/shared/build_deploy.py   # check 스크립트 연동
M  scripts/deploy.sh                             # check 연동 + 단계 보정
M  .planning/triage/INDEX.md                    # 신규 triage 등록
?? scripts/check-blog-issues.py                 # 신규 pre-build scanner
?? .planning/triage/20260710--blog-content-quality-enforcement.md
M  src/content/blog/ (194 files)                # **목차** 제거 + 1건 H1 수정
M  src/pages/my-persona.astro                    # (기존 변경 — 미커밋)
M  src/pages/persona/[...slug].astro             # (기존 변경 — 미커밋)
```
