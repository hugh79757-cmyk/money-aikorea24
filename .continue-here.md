# .continue-here.md — 세션 핸드오프

> 마지막 세션: 2026-07-09 22:50 (KST)
> 마지막 커밋: `2c58518` (auto-writer: title_generator.py 모듈 생성 + 파이프라인 연동)

## 현재 위치

**title_generator.py 신규 모듈 생성 + 파이프라인 연동 완료. Phase 5 이후 후속 작업.**

## 이번 세션 완료

| 작업 | 커밋 | 상세 |
|------|------|------|
| `title_generator.py` 신규 | `2c58518` | 10개 어미 카테고리, 가중치 선택, recency 추적, 총정리 10% 캡 |
| `writer.py:118` 강제 suffix 삭제 | `2c58518` | "총정리/완벽정리/한눈에/필독 중 택1" → 자연스러운 어미 지시 |
| `pipeline.py` 4b 연동 | `2c58518` | `refinish_title()` 호출 (신규 생성 글만) |
| npm build 검증 | — | 2459p, 0 errors |
| 내일 발행 dry-run 확인 | — | quota 5/5 소진, pending 5,234건, title_generator 정상 작동 확인 |

## 남은 작업

| 작업 | 우선순위 | 상태 |
|------|----------|------|
| 기발행 20건 타이틀 수정 여부 결정 (slug 동일 → 404 없음, 사용자 확정 필요) | ⏳ | 사용자 답변 대기 |
| `title_generator.py` `seed_recent()` 호출 (publish_ledger 기반 시드) | 낮음 | ⏳ 선택 |
| Phase 6 기획 | ⏳ | Phase 5 종료 후 |

## 주요 결정

| 결정 | 이유 |
|------|------|
| 10개 어미 카테고리 (정리/행동/질문/숫자/긴급/비교/결과/밀착/후기/시의성) | CTR↑ 검색의도 부합 |
| 가중치: 행동25/결과20/정리15/숫자10 | 행동유도·결과 중심 |
| `총정리` 10% 캡 (최근 10개 중 1회) | 완전 배제는 부자연스러움 |
| 기발행 20건 수정 안 함 (사용자 reversal) | "현재 발행된 글은 건들이지 않는다" |

## 블로커

- 없음. launchd 매일 09:00 자동 실행 — 내일 quota 리셋 후 신규 글부터 title_generator 적용됨.

## 재개 방법

```bash
cd /Users/twinssn/Projects/money-aikorea24
git log --oneline -5
npm run build      # SSG 빌드

# launchd 확인
launchctl list | grep aikorea24

# 수동 실행 (내일 quota 리셋 후)
python3 scripts/auto-writer/scheduler.py --status
python3 scripts/auto-writer/scheduler.py --dry-run
```

## 언커밋 변경

```
# 기발행 블로그 글 타이틀 수정본 (dry-run 결과, 커밋 안 됨)
 M src/content/blog/*.md (다수, frontmatter title만 변경)
# 로그 파일
 M scripts/auto-writer/logs/
 M scripts/manual-publisher/logs/
# 기능 파일 (신규, 미추적)
?? scripts/auto-writer/title_generator.py (이미 커밋됨 2c58518)
```
