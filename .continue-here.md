# .continue-here.md — 세션 핸드오프

> 중단 시각: 2026-07-29 22:15 KST
> 마지막 커밋: `df23ee0` — `feat(header): add income-percentile nav link`
> 배포 상태: 라이브 (`persona.aikorea24.kr`)

## 현재 위치

**GSD Phase 6 (Income Insights) 완료 후, 독립형 소득 백분위 계산기 페이지 구축 완료.** Phase 6은 원래 plan대로 4개 work item 모두 실행 완료된 상태였으며, 이 세션에서는 Phase 6을 넘어 새로운 `/tools/income-percentile/` 페이지를 처음부터 구축하여 배포까지 완료.

## 이번 세션 완료

| 작업 | 상태 | 커밋/근거 |
|------|------|-----------|
| **소득 백분위 계산기 페이지 생성** (`/tools/income-percentile/`) | ✅ | `afad5dd` |
| `calcTopPct()` 추출 → `src/lib/income-percentile.js` (shared module) | ✅ | `afad5dd` |
| 연봉/월소득 전환 토글 + 입력 검증 | ✅ | `afad5dd` |
| 게이지 시각화 (`.rank-gauge-*` CSS 재사용) | ✅ | `afad5dd` |
| URL 파라미터 공유 (`?salary=`, `?monthly=`) | ✅ | `afad5dd` |
| SEO (title/description/JSON-LD/sitemap) | ✅ | `afad5dd` |
| **FIX 1: URL 파라미터 검증 강화** — `?salary=abc`/`-5`/`0` → error, `calcTopPct` 미호출 | ✅ | `afad5dd` (built-in) |
| **FIX 2: Header.astro 변경 0줄 유지** (GO 전까지) | ✅ | `git diff HEAD -- src/components/Header.astro` 출력 없음 |
| **FIX 3: Client 스크립트 Vite 번들링** — `src/scripts/income-page.js`로 추출, 인라인 `import` 제거 | ✅ | `afad5dd` |
| Production smoke check (3 DOM 값 일치) | ✅ | 상위 30.4% / fill 69.6% / marker 69.6% |
| 배포 (초기) | ✅ | Cloudflare Pages |
| **TASK A: Inline CTA** — `[...slug].astro` (+monthly param) + `my-persona.astro` (plain link) | ✅ | `e674709` |
| **TASK B: Header nav link** — `📊 소득 계산` (desktop + mobile) with `nav-income` class | ✅ | `df23ee0` |
| 백분위 일치 검증 (3개 페르소나, gap 0.0pp) | ✅ | 보고서 제출 |
| **배포 (A+B)** | ✅ | 2 commits + production deploy |

## 남은 작업 / 후속 아이디어

- [ ] **benefits 페이지 URL param 필터** — Phase 6 A-1에서 계획되었으나 미완. `benefits/index.astro`가 `?age=&region=&income=` 파라미터를 읽도록 client-side JS 추가.
- [ ] **모바일 반응형 확인** — CTA 링크가 모바일 화면에서 깨지지 않는지 최종 확인.
- [ ] **AdSense 게시자 ID 중앙화** (ADS-05, deferred) — `consts.ts`에 통합 관리하도록 이전.
- [ ] **Card 이미지 income bar** (Phase 6 B-1/B-2) — 카드 재생성 스크립트에 income bar 추가 (실행은 별도).

## 주요 결정

| 결정 | 이유 |
|------|------|
| `calcTopPct()`를 `src/lib/income-percentile.js`로 추출 | `scripts/patch-income.mjs`와 Astro 페이지가 동일한 로직 공유 |
| `[...slug].astro` CTA에 `?monthly=${incomeVal}` pre-fill | gap 0.0pp로 불일치 없음 (동일한 calcTopPct 사용) |
| `my-persona.astro` CTA는 plain link | incomeVal이 Astro template scope에 없음 |
| CTA 스타일: `.income-recalc-link` small secondary border-link | 사용자 요청 "not a giant banner" |
| Header 네비 링크: `📊 소득 계산` with `.nav-income` class | `지원금`과 `커뮤니티` 사이 배치, 기존 CSS 그룹 재사용 |
| 배포 전 smoke check gate | persona.aikorea24.kr live URL에서 DOM 3개 값 직접 추출 |

## 블로커

- 없음.

## 버그 및 발견된 이슈

| 증상 | 원인 | 해결책 | 미해결? |
|------|------|--------|---------|
| `npm run dev`에서 `import { calcTopPct }` 구문이 `Uncaught SyntaxError` 발생 | Astro SSG (`output: 'static'`) 페이지의 `<script type="module">`에서 `import` 구문이 Vite 번들링되지 않음 | 클라이언트 스크립트를 `src/scripts/income-page.js`로 분리 (Vite가 `src/` 내 파일은 번들링함). 페이지에서는 `<script src="...">`로 참조 | 해결됨 |
| `--dump-dom`이 JS로 설정된 `textContent`를 출력하지 않는 경우 있음 | Chrome headless `--virtual-time-budget=5000`이 부족하면 JS 미실행 상태로 DOM 덤프 | `--virtual-time-budget=8000`으로 증가 → 정상 출력 확인 | 해결됨 |

## 환경 노트

- **macOS Sequoia**, Node v25.2.1, npm 11.x
- **Chrome headless** (`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`): `--dump-dom` + `--virtual-time-budget=5000~8000`으로 JS 렌더링 후 DOM 추출
- **Cloudflare Pages**: `env -u CLOUDFLARE_API_TOKEN npx wrangler pages deploy dist --project-name money-aikorea24 --branch main --commit-dirty=true`
- **빌드**: `npm run build` → `rm -f dist/persona-stats.json` (25MiB Cloudflare 제한 초과)
- **프로젝트 `AGENTS.md`**에 deploy.sh 설명 있으나 `CLOUDFLARE_API_TOKEN` env var 충돌로 수동 배포 사용
- `deploy.sh`는 `/Users/twinssn/Projects/5000/.env`에서 token을 로드하지만, OpenCode/Codex가 자동 설정한 token이 충돌함. `env -u CLOUDFLARE_API_TOKEN` 접근법이 안정적으로 동작 확인됨

## 재개 방법

```bash
cd /Users/twinssn/Projects/money-aikorea24

# 현재 상태 확인
git log --oneline -5
git status --short

# 로컬 개발
npm run dev

# 더 많은 스크린샷/검증이 필요하면:
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --headless --disable-gpu --no-sandbox --virtual-time-budget=5000 --dump-dom "http://localhost:4321/tools/income-percentile/?salary=5000" 2>/dev/null

# 배포 (변경 후):
npm run build && rm -f dist/persona-stats.json && git add -A && git commit -m "..." && git push && env -u CLOUDFLARE_API_TOKEN npx wrangler pages deploy dist --project-name money-aikorea24 --branch main --commit-dirty=true

# 추후 작업 예시: benefits URL param filter
# src/pages/benefits/index.astro 수정 필요
```

## 언커밋 변경

```
 M logs/manual-publisher.log          # 자동 생성 로그, git에 포함되지 않음
```

변경사항 없음 — 작업 분기점 clean.
